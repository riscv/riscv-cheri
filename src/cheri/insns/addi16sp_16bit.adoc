<<<

[#C_ADDI16SP_CAP, reftext="{C_ADDI16SP_CAP} ({cheri_base_ext_name})"]
==== {C_ADDI16SP_CAP} ({cheri_base_ext_name})

Synopsis::
{C_ADDI16SP_CAP_DESC}

Mnemonic::
`{C_ADDI16SP_CAP} nzimm`

Expansion::
`{caddi} {abi_creg}sp, {abi_creg}sp, nzimm`

Encoding::
include::wavedrom/c-int-reg-immed.adoc[]

Description::
Add the non-zero sign-extended 6-bit immediate to `{abi_creg}sp.address` (`{abi_creg}sp={creg}2`), where the immediate is scaled to represent multiples of 16 in the range (-512,496).
+
All other details, including the conditions for clearing `{abi_creg}sp.tag`, are as defined by <<CADDI>> for the expansion shown above.

Prerequisites::
{c_cheri_base_ext_names}

Included in::
<<rvy_zca_mod_insn_table>>

Operation::
[source,SAIL,subs="verbatim,quotes"]
----
SAIL TO BE UPDATED
----
//sail::execute[clause="C_ADDI16SP_capmode(_)",part=body,unindent]

IMPORTANT: Any instance of this instruction with a `cs1`=`y0` will raise an exception, as `y0` is defined to always hold a <<null-cap>> capability.
As such, the encodings with a `cs1`=`y0` are RESERVED for use by future extensions.

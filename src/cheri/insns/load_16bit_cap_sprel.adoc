<<<

[#C_LOAD_CAP,reftext="{C_LOAD_CAP_NAME}"]
==== {C_LOAD_CAP_NAME}

see <<C_LOAD_CAP_SP>>.

[#C_LOAD_CAP_SP,reftext="{C_LOAD_CAP_SP_NAME}"]
==== {C_LOAD_CAP_SP_NAME}

Synopsis::
Capability loads ({C_LOAD_CAP_NAME}, {C_LOAD_CAP_SP_NAME}), 16-bit encodings

ifndef::cheri_ratification_v1_only[]
include::xlen_variable_warning.adoc[]
endif::[]

Mnemonics::
`{c_load_cap_name_lc} {cd}', offset({cs1}')` +
`{c_load_cap_sp_name_lc} {cd}', offset({abi_creg}sp)`

Expansions::
`{load_cap_name_lc} {cd}', offset({cs1}')` +
`{load_cap_name_lc} {cd}', offset({abi_creg}sp)`

Encoding::
include::wavedrom/c-sp-load-cap.adoc[]

Description::
For <<C_LOAD_CAP>> the authorizing capability is `{cs1}`.
+
For <<C_LOAD_CAP_SP>>, the authorizing capability is `{abi_creg}sp`.
+
The effective address of the memory access is obtained by adding the address of the authorizing capability to the zero-extended offset.
+
Load data from memory in the same manner as <<LOAD_CAP>>.

Prerequisites::
{c_cheri_base_ext_names}

Included in::
<<rvy_zca_insn_table>>

Operation (after expansion to 32-bit encodings)::
See <<LOAD_CAP>>

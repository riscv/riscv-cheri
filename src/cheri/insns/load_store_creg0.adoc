IMPORTANT: Any instance of this instruction with `{cs1}`=`{creg}0` will raise an exception, as `{creg}0` is defined to always hold a <<null-cap>> capability.
As such, the encodings with `{cs1}`=`{creg}0` are RESERVED for use by future extensions.
